import { css } from '@emotion/css';

export const footer = css({
  borderBlockStart: '1px solid lightgrey',
  paddingBlock: 16,
});

export const wrapper = css({
  display: 'flex',
  alignItems: 'center',
});

export const sourceCodeLink = css({
  marginInlineStart: 'auto',
});
