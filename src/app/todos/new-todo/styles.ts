import { css } from '@emotion/css';

const wrapper = css({
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'space-between',
  gap: 16,
});

const description = css({ flex: 1 });

export { description, wrapper };
